# create a public key and secret key for the voting app

# create a poll with owner account

# check that poll cannot be createed by non owner account

# check that secret poll values are null

# create a voter account

# check that anyone can create a voter account

# check that a voter can vote

# check that voter cannot voter after the poll finishes

# reveal a poll

# check that only owner can reveal a poll

# cancel a poll

# check that only owner can cancel a poll

# check sending a voting time empty or previous to current time returns error while creating poll

# check empty vote returns error while voting or updating vote

# check non integer tokens staked or greater than a limit returns error while creating or updating vote